{
  "name": "rosefinch-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent runtime skeleton combining Pi (unified LLM stream + ergonomic state) with DeepSeek Harness (event-sourced log + waterfall plugins + tool scheduler).",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "demo": "tsx examples/demo.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}